{
  "name": "@ppe/routing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Where an app is mounted. Shared because the web server, Vite and every app's router must agree.",
  "exports": { ".": "./src/index.ts" },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.1",
    "vitest": "^5.0.1"
  }
}
