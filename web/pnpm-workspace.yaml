packages:
  - 'packages/*'
  - 'apps/*'
  - 'e2e'
  # _template is a recipe, not a package.
  - '!apps/_template'

# esbuild and similar ship platform binaries via install scripts; allow only these.
allowBuilds:
  esbuild: true

minimumReleaseAgeExclude:
  - lucide-react@1.48.0
  - vite@8.3.1
